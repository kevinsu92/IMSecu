# 결정 기록

세션이 바뀌어도 같은 조사를 반복하지 않기 위한 문서다. 결론과 근거만 적는다.
자세한 서술(`PLAN.md`)과 연구 근거(`research/`)는 작업 문서라 공개본에는 포함하지 않았다.

최종 갱신 2026-09-06 (일)

> 이 문서의 수치는 **현재 배포 설정(top2)** 기준이다. top3 시절 측정치를
> 그대로 두면 다음 세션이 이미 뒤집은 결정을 되돌린다. 실제로 그런 항목이
> 여럿 있었고 2026-09-06 에 일괄 교정했다.

---

## 시스템 성격 — 재조사 금지

| 항목 | 사실 |
|---|---|
| 언어·런타임 | Python 3.14.7 / numpy 2.5.2 / pandas 2.3.3 (Windows 11) |
| 증권사 API | **없다.** iM증권은 공개 API가 없다. 집행은 싸이칸플러스(axis.exe) **Win32 GUI 자동화**뿐이다 |
| 시세 데이터 | FinanceDataReader + 네이버 (`polling.finance.naver.com`, `siseJson`). 전부 무인증 |
| LLM 사용 | **0건.** 프로그램은 어떤 모델 API도 호출하지 않는다. 따라서 LLM 토큰 절감 대상이 없다 |
| 실행 구조 | `run.py plan` → `imrl/{universe,data,alpha,portfolio,turnover,state}` → 주문서 JSON → `execute.py` → `imrl/risk.py` 게이트 → `imrl/hts_exec.py` |
| 백테스트 | `backtest.py`. 421종목 × 935일 패널 캐시(`state/bars_cache.parquet`) |
| 스케줄 | 08:40~14:50 매시 HealthCheck / **15:05 Plan / 15:10 Sell / 15:21 Buy / 15:24·15:27 Buy 재시도** / 15:40 Reconcile / 16:00 Status (전부 RunLevel=Highest) |
| 운영 조건 | **사용자는 평일 낮에 PC 앞에 없다.** 무인 전제로 설계한다 |

**pykrx는 쓸 수 없다** — 2026년 기준 KRX 로그인을 요구한다. 라이선스 파일도 없다.

---

## 확정된 전략 결정

| 결정 | 값 | 근거 |
|---|---|---|
| 팩터 가중치 | `ret20 1.0 / ret5 -0.3 / amount_surge 0 / volatility 0.5` | 종가체결 실측에서 `역방향ret5`가 t 2.32~2.65로 상위 독식 |
| 시장경보 사전배제 | `alert_ratio < 0.85` | 규정 회피가 목적이었으나 성과도 개선. **현재 설정(top2) 재측정: t 1.92 → 3.37, 일평균 0.436% → 0.673%** (기존 기록의 2.32 → 3.66 은 top3 시절 수치다). 민감도 곡선이 완만한 단봉이고 기간 분할에서도 방향 일치 |
| 종목 수 | **top 2** | 목표가 "1등 아니면 의미 없음"이라 목적함수가 **우측 꼬리 도달 확률**이다. 실측 P(+50%) 14.8%(top3 11.4%), P(+80%) 3.7%(2.2%). P(<0)는 37.8% vs 37.2%로 사실상 동일 |
| 집행 시각 | **15:05 신호 / 15:10 매도 / 15:21 매수** | 매도·매수를 분리한다. 아래 "매도대금" 절 참조 |
| 지정가 | 매수 **+1%** / 매도 **−3%**, 제한폭 클램프 | ±1틱 체결률이 매수 62.1%/매도 51.2%. **비대칭인 이유가 서로 다르다** — 매도는 연속매매라 체결가가 내 지정가가 아니라 상대 최우선 매수호가이므로 오프셋이 진짜 공짜다. 매수는 동시호가인데 이 대회가 경매가 아니라 **1:1 미러링**이라 체결가 미명시 → 공짜라고 단정할 수 없다(1일차 판별 대상) |
| 수량 사이징 기준 | 매수 **지정가** (현재가 아님) | 현재가 기준이면 전량 상단 체결 시 예수금 1% 초과 → 거부 위험 |
| 회전율 파밍 자산 | CD금리/초단기채권 ETF (459580 / 469830 / 357870) | 기존 KOSPI200 4종은 일변동성 2.8%. **시장 베타는 순위를 못 바꾼다** — 전 참가자 공통 |
| 공격 국면 | **top2 / max_weight 0.45** / `volatility` 가중치 1.0 | 집중도는 이미 규정 상한이라 남은 레버가 변동성뿐이다. 1.0 이 P(+50%) 최대점(16.2%)이고 1.5 이상은 일평균·t 가 무너진다(3.34 → 2.81 → 1.97). **단 1.0 은 P(+80%)를 3.7% → 3.1% 로 낮춘다** — 1등은 더 깊은 꼬리에서 나오므로 0.5 유지가 나을 수 있다. 검정력 부족으로 미결 |
| 손절 | **없음** | 국내 장중 실측에서 가격 스탑은 조일수록 나빠진다(−2% < −3% < −5% < −8% < 스탑없음). 근거 있는 축은 보유일수뿐 |
| 오픈소스 도입 | **0개** | Python 3.14/numpy 2.5 위에서 데이터 계층이 동작 중. qlib·FinRL·vectorbt 중 하나라도 numpy를 내리면 참가 불가 |

### 자연 회전율 실측 (파밍 규모의 근거)

top2 일평균 종목 교체율 24.5% → 정의 B(매도/원금) 기준 20일 누적 **481%**.
요건 500%에 19%p 부족 → 부족분만 파밍한다. 정의 A라면 약 960%로 여유.

**실패 시나리오와 승리 시나리오가 양의 상관이다** — 고른 종목이 계속 상위를 유지하며
크게 오르면 랭킹이 안 바뀌어 회전이 멈춘다. 잘 되고 있을 때 실격당한다.

### 비용 민감도

| 왕복비용 | 일평균 | t | P(+50%) | P(+80%) |
|---|---|---|---|---|
| 0.22% (규정만) | 0.673% | 3.37 | 14.8% | 3.7% |
| 0.50% | 0.562% | 2.81 | 12.7% | 2.9% |
| 0.80% | 0.442% | 2.21 | 11.1% | 1.8% |

**P(+80%)가 P(+50%)보다 훨씬 빨리 무너진다** (3.7 → 1.8%, 반토막). 1등은 깊은 꼬리에서
나오므로 비용 오차의 실질 타격이 일평균 감소분보다 크다.

시장충격은 1:1 미러링이라 구조적으로 0 이다. 위 값은 충격이 아니라 **미체결 역선택**
(왕복 0.73%p 실측)을 반영한 시나리오다. 0.22% 단독 인용은 낙관이다.

---

## 목적함수 — 1등 단독 (2026-09-05 사용자 확정)

사용자가 "1등 아니면 의미가 없다"고 명시했다. 이건 동기 부여가 아니라 **설계 제약**이다.

- 목적함수가 기대수익도 P(수상)도 아니고 **P(최종 1위)** 하나다
- 1위 수익률은 참가자 전체의 **최댓값**이므로 우측 꼬리에 닿아야만 의미가 있다
- **마이너스 실격 조항은 1등 경로를 구속하지 않는다** — `P(내가 필드 최댓값 AND R<0) = 0.000000`.
  그 조항은 2~25위 경로에서만 작동한다. 따라서 손실 한도·방어는 1등 목표에서 순손실이다
- 결과: top3 -> **top2**, 방어 국면 **완전 비활성화**(`defend_enabled: false`)
- `max_drawdown_pct` 는 **40.0** 이다. 0(비활성)이 아니다 — 아래 주석 참조

우측 꼬리 실측 (경보필터 on, 종가체결, 909구간):

| 설정 | 중앙값 | P(+50%) | P(+80%) | P(+100%) | P(<0) |
|---|---|---|---|---|---|
| **top2 vol0.5 (phase_early)** | 11.3% | 14.8% | **3.7%** | 0.9% | 37.8% |
| **top2 vol1.0 (phase_attack)** | 9.9% | **16.2%** | 3.1% | 1.3% | 38.5% |
| top3 vol0.5 (이전) | 10.0% | 11.4% | 2.2% | 1.5% | 37.2% |
| top4 vol0.5 | 8.5% | 7.6% | 0.7% | 0.2% | 38.1% |

방향이 top2 > top3 > top4 로 **모든 변동성 가중치에서 일관**돼 단일 칸의 우연이 아니다.
다만 이것은 **알파 주장이 아니라 분산 선택**이다 — 기대수익은 오히려 낮아질 수 있다.

**남은 최대 불확실성은 필드 분산 sigma_f 다.** p1 이 여기에 100배 달라지는데
아직 미측정이다. `run.py field --returns ... --n ...` 로 09/08부터 매일 기록할 것.

## 매뉴얼 확정 사항 (PDF 직접 확인, 2026-09-05)

PDF 가 스캔 이미지라 텍스트 추출이 안 된다. PyMuPDF 로 렌더링해 직접 읽었다.

**p11 거래일 및 매매시간**
- 동시호가 08:30~09:00, **15:20~15:30** / 정규시장 09:00~15:20
- **예약주문도 15:20~15:30 으로 같은 시간대에 표기돼 있다** — 우리 집행 시각과 겹치는
  모호함이 매뉴얼에 실재한다. 1일차에 주문 접수 유형을 캡처로 확인할 것
- 시간외거래(NXT/KRX) 미지원. 현금매도/매수만, 미수·신용·공매도 불가

**p12 수수료 및 제세금** — 설정과 일치 확인
- 거래소 0.01% (제세금 거래세 0.05% + 농특세 0.15% = 0.20%)
- 코스닥 0.01% (제세금 거래세 0.20%)
- **ETF 0.01% (제세금 없음)**
- 주문 유형은 **지정가·시장가만** 사용 가능
- **위탁증거금: 종목별 차등증거금제.** 최대주문가능금액이 여기서 산출된다.
  급등주는 증거금률이 높을 수 있어 주문가능금액이 예상보다 작을 가능성 — 1일차 확인 필요

**p13 순위결정 — 가장 중요**
```
총평가금액 = D+1일예수금 + 당일매도금액 - 당일매수금액 + 유가증권 평가금액
             - 당일 수수료 및 제세금
투자수익률 = (총평가금액 - 투자원금) / 투자원금 x 100
```
- **당일매도금액이 당일 반영된다.** 매도대금을 같은 날 쓸 수 있다
- 수익률 분모가 평가금액이 아니라 **투자원금**이다 — `build_targets` 가 원금 기준으로
  캡을 거는 것이 맞다

**p13 무효처리** — "고의적인 행위로 선의의 참가자의 게임참여와 공정한 게임운영을 방해한
경우" 당일 대회내용 무효. 회전율 파밍이 조작으로 보이면 안 되는 이유다.

## 매도대금 — 매도와 매수를 분리한 이유

매뉴얼상 당일매도금액은 당일 반영되지만, **종가 동시호가는 주문을 전부 접수한 뒤
15:30 에 한꺼번에 체결한다.** 즉 15:21 에 매수를 넣는 시점에는 매도가 아직 체결되지
않아 매도대금이 예수금에 없다.

실측 계산 (보유 90%, 종목 1개 교체):
```
보유평가 89,987,950원   예수금 10,012,050원
매수 총액 44,996,250 / 매도 총액 44,962,620
매도대금 없이 가능한가: X 부족 34,984,200원
```

1일차는 현금이 가득해 통과하지만 **2일차부터 리밸런싱이 통째로 실패한다.**

해법: **매도를 연속매매(15:10)에 먼저 내보내 즉시 체결시키고, 매수만 종가
동시호가(15:21)에 넣는다.** 부수 효과로 동시호가 매도 체결률 51.8% 문제도 피한다.

## 알파에 대한 정직한 평가

- 알파가 **2023년에 편중**된다. 2023년 이전 일평균 +1.009%/t 2.91, 2024년 이후 +0.541%/t 2.23.
  (기존 기록의 "2024년 이후 t=0.53"은 **경보필터 적용 이전** 수치다)
- 32회 시도 후의 t 는 귀무가설 하 `E[max|t|]`=2.07 과 비교해야 한다. 다중검정 부채가 크다
- **다중검정 부채가 없는 증거는 무작위 대조 하나다** (1000회, seed 20260906, top2 기준):
  신호 +0.673%/일 vs 무작위 평균 −0.060%/일, 무작위 최대 +0.227%/일,
  이긴 시행 **0/1000 (p=0.001)**. 20일 꼬리 P(+50%) 14.8% vs 0.6%
- **경보필터 의존도**: 필터를 빼면 t 3.37 → **1.92** 로 5% 유의에 못 미친다.
  규정 회피 목적으로 넣은 것이라 사후선택 편의는 아니지만, 알파가 얇다는 증거다

정확한 표현은 **"약한 횡단면 틸트를 20일 순위 대회의 보상 구조에 맞춰 2종목
고변동성 베팅으로 의도적으로 변환한 장치"**다. 순위 대회에서는 합리적 선택이지만
검증된 알파가 아니다.

> 재현: `python tools/verify_claims.py`, `tools/verify_alert.py`, `tools/verify_cost.py`
> → `state/backtest_current.json`

---

## 백테스트 신뢰도

- **미래 데이터 참조: 없음.** `backtest.audit_lookahead()` 로 자동 검사(i일 이후 데이터를 파괴해도 점수 불변). 12개 시점 전부 통과
- **구조적 편향은 남아 있다** — 유니버스를 오늘 기준 거래대금 상위로 뽑아 2년 과거에 소급 적용(전방선택 + 생존편향). 시점별 상장목록이 필요한데 pykrx가 막혀 해결 못 함
- 수수료·제세금 반영됨(0.22%). 슬리피지 미반영, 시나리오로만 병기

---

## 남은 작업

| # | 항목 | 상태 |
|---|---|---|
| 1 | HTS 모의투자 로그인 | **완료.** 헬스체크 통과, 계좌 `0000-0000-00 필명A` 확인 |
| 2 | 드라이런 전체 재검증 | **통과 (2026-09-05 20:21).** 3/3 성공. 종목·수량·가격·주문금액·탭·체크박스 전부 정확. 아래 표 참조 |
| 3 | 드라이런 소요시간 | **22초 / 3건** (건당 7.3초). 8건 환산 약 59초 → 15:21 단일 집행 성립 |
| 10 | 주문금액 자릿수 검증 | **완료 (경고 전용).** 실HTS 4건 전부 `자릿수 일치`. 1주일 관측 후 차단 승격 판단 |
| 11 | 매도 경로 | **검증 완료 (2026-09-05 20:28).** 매도 탭·지정가·주문금액 전부 정확. 단 검증 당시 오프셋은 −1% 였고 현재 설정은 **−3%** 다. 경로는 같다 |
| 4 | 체결내역 화면 파싱 | 미구현. `run.py reconcile` + 수동 `fill` 로 우회 중 |
| 5 | 주문 취소 경로 | 미구현 |
| 6 | `max_drawdown_pct` | **40.0 확정 (0.0 아님).** 이 값이 두 가지 일을 겸하고 있던 것이 문제였다. 전략 브레이크로 보면 1등 목표에서 순손실이 맞다(위 논리). 그러나 **버그 서킷브레이커**로는 필요하다 — 좌표 사고나 수량 폭주를 잡을 마지막 방어선이 없어진다. 전략 분포의 20일 P10 이 −32% 라 40% 는 전략 경로를 사실상 건드리지 않고 폭주만 잡는다. 매도는 어떤 값에서도 허용되므로 회복 경로도 막지 않는다 |
| 12 | 필드 분산 sigma_f 측정 | `run.py field` 구현 완료. **09/08부터 매일 입력.** 소급 취득 불가 |
| 7 | 회전율 산정 정의 A/B 확인 | **1일차는 매수만 발생**하므로(보유 0 시작) 중계실 회전율이 약 90%면 정의 A, 0%면 정의 B. 별도 실험 불필요 |
| 13 | **★ 1일차 수익률로 체결가 판별** | 매뉴얼 p14: 모의투자는 경매가 아니라 **실제시장 체결 1:1 미러링**. 체결 가격 미명시. 지정가에 체결되면 오프셋 1%가 20일 원금의 **18%**. 1일차는 매수만 발생하므로 수익률 -0.01% vs -1% 로 판별 |
| 14 | **매도대금 즉시 매수 재사용 가능 여부** | **최우선.** 매뉴얼 p13 은 당일매도금액을 당일 반영(평가 기준), p15 는 **"3일 결제"**(결제 기준). 두 층위가 다르다. 안 되면 2일차부터 리밸런싱 실패 → T+1 리밸런싱으로 전환 |
| 8 | 중계실 상위 100명 수익률 기록 | 09/08 필수. 소급 취득 불가 |
| 9 | 15:20~15:30 주문이 당일 체결인지 익일인지 | 09/08 1일차 캡처로 확인 |

**09/05는 토요일이다. 다음 장이 곧 대회 1일차(09/08)이므로 장중 리허설 기회가 없다.**

---

## 사고 이력 (같은 실수 반복 방지)

| 사고 | 원인 | 대응 |
|---|---|---|
| 사용자 채팅창에 `1200`, `2400` 등이 타이핑됨 | `SetForegroundWindow` 성공 반환했으나 포커스는 다른 곳 | 모든 전역 입력 전 `assert_focus`, 실패 시 입력 안 함 |
| 종목이 이전 값 그대로 남은 채 수량·가격만 바뀜 | WM_CHAR로 넣으면 앱의 종목조회가 안 돎 | 세 칸 전부 스캔코드 입력으로 통일 + `read_edit` 대조 |
| `SendMessage(WM_LBUTTONDOWN)`로 axis.exe 크래시 | 마우스 캡처 루프에 갇힘 | 실제 마우스 입력 + 포커스 가드 |
| 드라이런이 "4건 성공"인데 캡처는 전부 000660 | `symbol_rect` 외삽이 칸을 빗나감 | `find_symbol_edit()` 컨트롤 열거로 확정 |
| 스케줄러 실행 시 주문 4건 전부 거부 | python 콘솔이 포그라운드를 뺏음 | `hide_own_console()` |
| 15:20 집행 리포트가 `⚠`에서 죽음 | 콘솔 cp949 | stdout/stderr UTF-8 고정 |
| 백테스트 통계 전부 `inf` | 시가 패널에 0이 섞임 | `entry > 0 & exit > 0` 필터 |
| "관리자 권한 확인" 오류가 원인을 오지목 | 실제 원인은 HTS **화면잠금** | `session_state()` 로 lock/not_mock 구분 + 14:50 헬스체크 |
| 중복 방지가 뚫림 | `order_key`에 가격이 들어 있어 1틱만 달라도 새 키 | `side:code` 로 축소 |
| 상한가 종목 비중이 통째로 현금 | 스킵만 하고 재분배 없음 | 남은 종목에 비례 배분(규정 45% 클램프) |
| 스케줄러 작업에 인자가 하나도 안 들어감 | `New-ImrlTask` 의 파라미터 이름이 `$Args` 였다. **PowerShell 자동 변수와 충돌해 조용히 빈 문자열이 된다.** IMRL_AutoTrade 가 `--live` 없이 등록돼 월요일에 드라이런만 돌 뻔했다 | 파라미터를 `$TaskArgs` 로 개명 |
| HTS 최소화 시 133x51 엉뚱한 창을 메인으로 잡음 | **최소화된 창도 `IsWindowVisible`=True** 이고 rect 만 (-32000,-32000) 근처가 된다. "가장 큰 보이는 창" 규칙이 무너진다 | `find_main` 을 **MDIClient 자식을 가진 창**으로 판정. 크기·최소화 무관하게 정확 |
| 전송 버튼이 버튼 아래 상태바를 가리킴 | 비율 외삽 배수가 4.69배라 가격칸 3% 변화가 63px 오차로 증폭 | 파란색 직접 탐지(`find_submit_button`) |

---

## HTS GUI 좌표 — 2026-09-05 실측 확정

주문창을 **2453x1514 고정**으로 못박은 상태 기준(`pin_order_window`).
가격칸(노란 박스) = (1346, 354, 1550, 410), 크기 204x56.

| 항목 | 옛 계산값(틀림) | 실측(맞음) | 증상 |
|---|---|---|---|
| 자동(현재가) 체크박스 | (1509, 436) | **(1361, 438)** | 148px 오른쪽 빗나감 → "해제하지 못했다" 무한 실패 |
| 시장가 체크박스 | (1731, 383) | **(1753, 380)** | 옆 '호' 버튼 모서리를 눌러 시장가가 켜짐 |
| 전송 버튼 | (2303, 707) | **(2272, 644)** | 버튼 **아래 상태바**에 떨어짐 → 눌러도 전송 안 됨 |
| 매수 탭 | (1293, 140) | **(1293, 183)** | y 43px 위 |

**근본 원인은 비율 외삽이다.** 전송 버튼 오프셋이 가격칸 폭의 4.69배라, 가격칸이
198 -> 204(3%)로 커지자 오차가 63px로 증폭됐다. 그래서 **전송 버튼은 비율을 버리고
파란색으로 직접 찾는다**(`hts_anchor.find_submit_button`). 가격칸을 노란색으로 찾는
것과 같은 원리다. 나머지는 실측 비율로 교정했다.

### 시장가가 켜지면 앵커가 통째로 사라진다

시장가 체크 시 가격칸이 **회색 0**이 되어 노란 박스가 화면에서 없어진다.
그러면 `find_price_box`가 실패하고 모든 실행이 "가격 입력칸을 찾지 못했다"로 죽는다.
좌표가 어긋난 클릭 한 번이 이 상태를 만들었다. 복구는 `execute.py --reset-form`
(앵커가 없으므로 실측 절대좌표로 시장가를 해제한다).

### 수량·가격은 되읽을 수 없다 (측정 결과)

주문창의 Edit 컨트롤은 **총 4개, 비-readonly 2개이고 둘 다 종목칸**이다.
수량·가격·주문금액은 자식 창이 없어 `WM_GETTEXT` 대상이 아니다. 따라서
"컨트롤을 찾아 값을 대조한다"는 검사는 **원리적으로 통과 불가**다.

대신 입력 전후 **픽셀 해시 변화**로 "그 칸에 입력이 실제로 들어갔는지"를 본다.
과거 사고는 전부 "칸을 빗나가 이전 값이 그대로 남음" 형태라 이 검사로 잡힌다.
잡지 못하는 것은 **올바른 칸에 틀린 숫자**이고, 그건 스캔코드 입력 경로가
결정적이라는 점과 전송 캡처를 사람이 보는 것으로만 방어된다. 숨기지 않고 적어둔다.

### 드라이런 통과 기록 (2026-09-05 20:21, 3/3)

| 종목 | 시장 | 수량 | 가격 | 주문금액 | 검산 |
|---|---|---|---|---|---|
| 003010 혜인 | 코스피 | 3,035 | 10,760 | 32,656,600 | 일치 |
| 092870 엑시콘 | 코스닥 | 989 | 33,000 | 32,637,000 | 일치 |
| 002990 금호건설 | 코스피 | 2,010 | 16,250 | 32,662,500 | 일치 |

매수 탭 활성 / 시장가 해제 / 자동(현재가) 해제 / 보통가 / 시장 구분 자동 인식 /
종목명 조회 정상.

**매도 경로도 검증** (20:28): 003010 혜인 10주 @ 10,540 (현재가 10,650 에서 −1%),
주문금액 105,400 일치, 매도 탭 활성, 대여매도 해제, 매도(F12) 버튼 표시.

**매수·매도 입력 경로가 모두 검증됐다.**

> **주문금액 칸이 수량 × 가격을 교차검증한다.** 수량·가격은 되읽을 수 없지만
> 이 한 숫자는 둘 다에 의존한다. 현재 픽셀 변화 검사는 "입력이 그 칸에 들어갔는가"만
> 보므로 **"3,035 를 치려다 3,03 만 들어간" 경우를 못 잡는다.** 주문금액 영역
> 숫자 템플릿 매칭이 그 구멍을 막는다. 폰트·위치가 고정이라 가능하다.
> 단 **경고 전용으로 먼저 도입한다** — 차단으로 넣었다가 오탐이 나면 그날 매매가 날아간다.

### 모의세션 판정은 제목이 아니라 계좌번호로

재로그인 후 메인 창 제목이 `(S071)모의시장` -> `싸이칸 Plus` 로 바뀌어
`MOCK_MARKER` 검사가 정상 세션을 차단했다. 판정 기준을
`config.contest.mock_account`(= `0000-0000-00`) 대조로 바꿨다.
"아무 모의계좌"가 아니라 "대회용 그 계좌"인지까지 보므로 더 강한 검사다.

### 최소화 상태 동작 (2026-09-05 실측)

| 무엇이 최소화 | 결과 |
|---|---|
| **HTS 최소화** | **동작한다.** `ensure_main_size`/`pin_order_window` 가 `IsIconic` 이면 먼저 복원한다. 최소화 상태로 헬스체크를 돌려 통과 확인. 단 **최소화된 채로 있지는 않는다** — 스스로 튀어나와 화면 맨 앞을 약 40초 차지한다 |
| **다른 앱(Claude 등) 최소화** | **이래야만 동작한다.** 덮여 있으면 전 주문 거부(실측: 3건 전부) |
| Windows 잠금 화면 / HTS 화면잠금 | 동작 불가. 화면이 렌더되지 않아 `PrintWindow` 가 0 을 돌려준다 |

즉 "최소화해두면 조용히 백그라운드로 돈다"는 성립하지 않는다.
집행 순간에는 반드시 HTS 가 화면 맨 앞에 나온다.

### 백그라운드 실행은 현재 구조로 불가능하다

수량·가격 칸이 커스텀 렌더링이라 `WM_SETTEXT`/`WM_CHAR`가 무시되고
`PostMessage` 클릭은 axis.exe 를 크래시시켰다. 남은 입력 경로가
`keybd_event`/`mouse_event` **전역 입력**뿐이라 HTS 가 화면 맨 앞에 있어야 한다.

실증: 클릭 지점을 Claude 데스크톱 창이 덮어 `WindowFromPoint` 가드가 클릭을
거부했다(`클릭 지점 (1980,839) 의 창이 주문창이 아니다`). **실패 방향은 안전하다** —
엉뚱한 창에 입력하는 것이 아니라 거부된다.

**2026-09-05 조사 완결** (`research/background_execution/FINDINGS.md`).
선택지가 셋인 줄 알았는데 실측 결과 **둘이 아예 존재하지 않는다.**

| 방법 | 판정 | 이유 |
|---|---|---|
| 다중 데스크톱 | **불가(실측)** | 비입력 데스크톱에서 `SendInput`·`keybd_event` 둘 다 `ERROR_ACCESS_DENIED(5)` |
| RDP + `tscon` | **불가(실측)** | 이 PC 는 **Windows 11 Home**. `tscon.exe`·`qwinsta.exe` 가 설치돼 있지 않다 |
| Hyper-V / Sandbox | **불가(실측)** | `vmms.exe`·`vmcompute.exe`·`WindowsSandbox.exe` 전부 부재 (Home 미지원) |
| UIA / MSAA | **불가(실측)** | HTS 바이너리 84개에 UIA 제공자 **0건** |
| VM (VirtualBox) | 원리상 가능, 비권장 | 라이선스 + HTS 재설치 + 보안모듈 VM 탐지 리스크 |
| **별도 물리 PC** | **가능** | 유일하게 깨끗. 단 **동시 로그인 정책 확인 필수** |
| **현행 시간 격리** | **가능·검증됨** | 3건 22초, 하루 1분 미만 |

**결론: 대회 내내 시간 격리로 간다.** 여분 PC 가 있으면 무장중에만 병행 구축한다.

### 무인 운영 위험 점검 (2026-09-05 실측)

| 위험 | 상태 |
|---|---|
| HTS 자동 화면잠금 | **없음.** `HKCU\Software\cyKhanPlus\AXIS Workstation V04.00\Workstation` 의 `IdleTimeout = 0`. 09/05 에 걸린 잠금은 사람이 잠근 것이지 앱이 스스로 잠근 것이 아니다 |
| Windows 자동 잠금 | 없음 (`InactivityTimeoutSecs` 정책 없음) |
| 절전 · 모니터 끄기 | 둘 다 0(안 함) |
| 화면보호기 | `SCRNSAVE.EXE` 미설정 |
| **HTS 세션 만료** | **미확인.** 장시간 유휴 후 세션이 끊기는지 09/08 관측 필요 |
| **Windows Update 재부팅** | **미대응.** 대회 기간 업데이트 일시중지를 검토할 것 |
| HTS 로그아웃 | 복구 불가 — 비밀번호 입력이 필요하고 자동화가 대신할 수 없다. 08:40/12:30 점검이 조기 경보 역할 |

### 집행 창(15:10~15:30) 운영 수칙

- **텔레그램이 두 번 알린다** (2026-09-07 추가, `watchdog.ensure_hands_notice`):
  15:08 "손 떼기"(오늘 매도·매수 건수 포함), 그리고 매도·매수 단계가 모두 끝나면
  "컴퓨터를 써도 된다"(접수·미접수·불명·미전송 조각 집계 포함). 끝 알림은 늦어도
  15:30 에 나간다. 재시도 작업은 원장에 남은 조각이 없으면 `connect()` 전에 끝나므로
  끝 알림 뒤에는 화면을 건드리지 않는다.
- **HTS 를 최소화하지 않는다.** 2026-09-07 실측: 최소화된 HTS 는 헬스체크
  (08:40, 09:30~13:30 매시, 14:50)가 복원·최대화하면서 약 10초간 화면 맨 앞으로
  나온다. **최대화한 채 다른 창 뒤에 두면** 같은 경로(`OrderAdapter.connect`)가
  포그라운드를 바꾸지 않는다(`tools/probe_connect.py`, 1.8초, 포그라운드 그대로).
  집행 창 밖에서 컴퓨터를 쓰는 조건은 이것 하나다.
- **PC 를 잠그지 않는다.** 잠금은 입력 데스크톱을 `Winlogon` 으로 바꾸므로
  위 표의 `err 5` 와 **같은 실패**다. 겪었던 "화면잠금" 사고의 정체가 이것이다.
- **always-on-top 앱을 띄우지 않는다.** `WindowFromPoint` 가드가 거부하므로
  오주문은 안 나지만 **주문을 놓친다.**
- 전원 설정 확인함(2026-09-05): 모니터 끄기 0(안 함) / 절전 0(안 함) /
  화면보호기 미설정 / 자동잠금 정책 없음 — **추가 조치 불필요.**

## 2026-09-08 1일차 사고 — 매매 0건. 원인 셋, 조치 넷

| 원인 | 무엇이 일어났나 | 조치 |
|---|---|---|
| **종목목록 출처 404** | `fdr.StockListing("KRX")` 은 KRX 가 알려주는 최근 영업일(장중엔 오늘)의 GitHub 캐시 CSV 를 읽는데, 그 파일은 장 마감 뒤에 생긴다. 14:45 뉴스회차·15:05 계획(7회 재시도) 전부 404 → 유니버스 없음 → 주문서 없음 | `data.load_listing()` 을 (1) 캐시 CSV 의 **있는 최근 날짜** (2) FDR (3) 네이버 시가총액 목록(ETF 제외, 억원 단위) (4) 로컬 캐시 `state/listing_cache.csv` 순으로 바꿨다. 실데이터 섀도 계획 통과(2873종목, 기준일 전일) |
| **HTS 화면잠금** | 08:40 점검 정상 → 09:30~14:50 매시간 "주문창 위에 팝업: 싸이칸 Plus-화면잠금". 레지스트리 IdleTimeout=0 이라 앱 자동잠금은 아니다. 09시 전후에 사람이 PC 나 HTS 를 잠갔을 가능성이 크다(사용자 확인 필요). 재로그인이 필요했던 다른 원인은 09-07 22:35·09-08 00:26 두 번의 사용자 재시작이다 | 감시자가 HTS 준비→미준비 전환 순간 텔레그램 경보(30초 폴링), 준비 복귀도 알림. 규칙 재확인: PC 잠그지 않기, 재부팅하지 않기 |
| **텔레그램 요청 과다(429)** | retry_after 300~570초를 그대로 기다려 계획 재시도가 6~9분씩 밀리고 점검 경보가 늦게 닿았다 | 429 대기 상한 10초(넘으면 포기·로그), 같은 글 20분 억제(`state/notify_recent.json`), 전송 기록 `state/notify_log.jsonl` |
| (요청) 밖에서 통제 | 상황판은 이 PC 안에서만 열린다 | 제어 서버에 **텔레그램 명령** 스레드: /상태 /정지 /재개 /건너뛰기 /익절 /복구 /제안 /제안목록 /철회 /상황판(HTML 파일 전송) /갱신 /도움. 설정된 chat_id 만 듣는다. 상황판의 손잡이와 같은 함수 |

"특정 시각까지 기다리지 말고 즉각 매매하라"에 대한 답: 감시자는 원래 **상태 기반**이다 — 창(매도 15:10~15:19, 매수 15:21~15:28) 안에서 HTS 가 준비되는 순간 밀린 단계를 바로 실행한다(30초 폴링, 재시도 90초). 장중 임의 시각 매매는 측정된 알파(종가 매수→오버나잇)를 버리는 것이라 하지 않는다. 오늘 실패는 시각이 아니라 데이터(404)와 잠금이었다.

상황판을 밖에서 **라이브로** 보려면 Tailscale(사설망) 설치가 필요하다 — 사용자 계정 작업이라 사용자가 한다. 그 전까지는 /상황판 명령이 그 시각의 HTML 을 보낸다.

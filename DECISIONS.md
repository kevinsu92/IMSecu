# 결정 기록

시스템의 형태를 정한 결정과 그 이유만 적는다. 수치는 현재 배포 설정(top2) 기준이다.

## 시스템 성격

| 항목 | 사실 |
|---|---|
| 런타임 | Python 3.12+ (개발 3.14), Windows |
| 증권사 API | 없다. 집행은 싸이칸 Plus(axis.exe) Win32 GUI 자동화뿐이다 |
| 시세 | FinanceDataReader + 네이버(`siseJson`), 전부 공개 시세(로그인 불필요). pykrx 는 KRX 로그인을 요구해 쓰지 않는다 |
| LLM | 프로그램은 어떤 모델 API 도 호출하지 않는다 |
| 실행 구조 | `run.py plan` → `imrl/{universe,data,alpha,portfolio,turnover,state}` → 주문서 JSON → `execute.py` → `imrl/risk.py` 게이트 → `imrl/hts_exec.py` |
| 운영 전제 | 무인. 장중에 사람이 PC 앞에 없다고 가정한다 |

## 목적함수 — 최종 1위 단독

- 목적함수는 기대수익도 P(입상)도 아닌 **P(최종 1위)** 하나다. 1위 수익률은 참가자 전체의 최댓값이므로 우측 꼬리에 닿아야만 의미가 있다.
- 마이너스 실격 조항은 1등 경로를 구속하지 않는다(P(필드 최댓값 AND R<0) ≈ 0). 손실 한도·방어 국면은 1등 목표에서 순손실이므로 `defend_enabled: false`.
- 이 선택은 알파 주장이 아니라 **분산 선택**이다. 기대수익은 오히려 낮아질 수 있다.
- 남은 최대 불확실성은 필드 분산 sigma_f 다. `run.py field` 로 매일 기록한다(소급 불가).

우측 꼬리 실측 (경보필터 on, 종가체결, 909구간):

| 설정 | 중앙값 | P(+50%) | P(+80%) | P(<0) |
|---|---|---|---|---|
| top2 vol0.5 (현행) | 11.3% | 14.8% | 3.7% | 37.8% |
| top2 vol1.0 (공격) | 9.9% | 16.2% | 3.1% | 38.5% |
| top3 vol0.5 | 10.0% | 11.4% | 2.2% | 37.2% |
| top4 vol0.5 | 8.5% | 7.6% | 0.7% | 38.1% |

## 전략 결정

| 결정 | 값 | 이유 |
|---|---|---|
| 종목 수 | top 2 | 우측 꼬리 도달 확률 최대. P(<0)는 top3 와 사실상 같다 |
| 집중도 | 49% 캡(원금 기준), 현금 완충 5% | 단일 종목 50% 규정 상한. 수익률 분모가 투자원금이라 캡도 원금 기준이다 |
| 팩터 가중치 | ret20 1.0 / ret5 −0.3 / amount_surge 0 / volatility 0.5 | 종가체결 실측에서 역방향 ret5 가 t 2.3~2.6 으로 상위 |
| 시장경보 사전배제 | `alert_ratio < 0.85` | 규정 회피가 목적이었으나 성과도 개선(t 1.92 → 3.37, 일평균 0.436% → 0.673%) |
| 공격 국면 변동성 가중치 | 1.0, 미결 | P(+50%) 최대점(16.2%)이지만 P(+80%)는 3.7 → 3.1% 로 낮아진다. 검정력 부족 |
| 손절 | 없음 | 장중 실측에서 가격 스탑은 조일수록 나빠진다(−2% < −3% < −5% < −8% < 없음) |
| 회전율 파밍 자산 | CD금리·초단기채 ETF (459580 / 469830 / 357870) | 시장 베타는 전 참가자 공통이라 순위를 못 바꾼다. 일변동성 2.8% 인 KOSPI200 ETF 는 쓰지 않는다 |
| 파밍 규모 | 부족분만 | 자연 회전율 20일 누적 481%(매도/원금 기준), 요건 500%. 고른 종목이 잘 될수록 랭킹이 안 바뀌어 회전이 멈추므로 파밍이 필요하다 |
| 오픈소스 도입 | 0개 | qlib·FinRL·vectorbt 가 numpy 를 내리면 참가 불가 |
| `max_drawdown_pct` | 40.0 | 전략 브레이크가 아니라 버그 서킷브레이커. 전략 20일 P10 이 −21% 라 폭주만 잡는다. 매도는 항상 허용 |

비용 민감도: 왕복 0.22%(규정만) → 0.80% 에서 P(+80%)가 3.7% → 1.8% 로 반토막 난다. 1:1 미러링이라 시장충격은 0 이고 실질 비용은 미체결 역선택(왕복 0.73%p 실측)이다.

## 집행 시각 — 매도 15:10, 매수 15:21

| 결정 | 값 | 이유 |
|---|---|---|
| 스케줄 | 15:05 계획 / 15:10 매도 / 15:21 매수 / 15:24·15:27 매수 재시도 / 15:40 대조 / 16:00 보고 | 종가 매수 → 오버나잇이 측정된 알파. 장중 임의 시각 매매는 하지 않는다 |
| 매도·매수 분리 | 매도는 연속매매, 매수만 종가 동시호가 | 동시호가는 15:30 에 일괄 체결되므로 15:21 매수 시점에 매도대금이 예수금에 없다. 보유 90%·1종목 교체 시 약 3,500만원 부족 → 2일차부터 리밸런싱 실패. 부수 효과로 동시호가 매도 체결률 51.8% 문제도 피한다 |
| 지정가 | 매수 +1% / 매도 −1%, 제한폭 클램프 | 매도는 연속매매라 체결가가 상대 최우선 매수호가여서 오프셋이 공짜. 매수는 1:1 미러링이라 체결가 미명시 — 지정가에 체결되면 오프셋 1% 가 20일 원금의 18% 라 판별 대상 |
| 수량 사이징 | 매수 지정가 기준 | 현재가 기준이면 상단 체결 시 예수금 초과로 거부 |
| 상한가 종목 | 남은 종목에 비례 재분배(49% 클램프) | 스킵만 하면 그 비중이 통째로 현금 |
| 주문 유형 | 지정가만 | 매뉴얼상 지정가·시장가만 가능. 시장가는 화면 앵커를 없애므로 금지 |

매뉴얼 확정 사항: 총평가금액 = D+1 예수금 + 당일매도 − 당일매수 + 유가증권 평가 − 당일 수수료·제세금, 수익률 분모는 투자원금. 수수료 0.01%, 제세금 거래소·코스닥 0.20% / ETF 없음. 종목별 차등증거금제. 무효처리 조항이 있으므로 회전율 파밍이 조작으로 보이면 안 된다.

## HTS 자동화 제약

| 결정 | 이유 |
|---|---|
| 입력 경로는 전역 입력(`keybd_event`/`mouse_event`)뿐 | 수량·가격 칸이 커스텀 렌더링이라 `WM_SETTEXT`/`WM_CHAR` 가 무시되고, `PostMessage` 클릭은 axis.exe 를 크래시시킨다 |
| 집행 순간 HTS 가 화면 맨 앞이어야 한다 | 다른 창이 덮으면 `WindowFromPoint` 가드가 거부한다. 실패 방향은 안전하다 — 오주문 대신 주문 누락 |
| 백그라운드 실행 불가, 시간 격리로 간다 | 다중 데스크톱(`ERROR_ACCESS_DENIED`), RDP/tscon·Hyper-V(Home 미지원), UIA(제공자 0건) 전부 실측 불가. 3건 22초라 하루 1분 미만이면 충분하다 |
| 주문창 2453x1514 고정(`pin_order_window`) | 좌표 재현성 |
| 가격칸은 노란색, 전송 버튼은 파란색으로 직접 탐지 | 비율 외삽은 가격칸 3% 변화가 63px 오차로 증폭됐다 |
| 메인 창은 MDIClient 자식을 가진 창으로 판정 | 최소화된 창도 `IsWindowVisible`=True 라 "가장 큰 보이는 창" 규칙이 무너진다 |
| 종목·수량·가격 전부 스캔코드 입력 + 입력 전후 픽셀 해시 대조 | 수량·가격은 Edit 컨트롤이 없어 되읽을 수 없다. 픽셀 검사는 "칸을 빗나감"을 잡고, "옳은 칸에 틀린 숫자"는 주문금액 자릿수 검사(경고 전용)로 보완한다 |
| 모의세션 판정은 계좌번호 대조 | 재로그인 후 창 제목이 바뀌어 제목 검사가 정상 세션을 차단했다 |
| 중복 방지 키 `side:code` | 가격이 키에 들어가면 1틱만 달라도 새 주문이 된다 |
| 모든 전역 입력 전 `assert_focus`, 자체 콘솔 숨김 | 포커스가 딴 곳이면 다른 앱에 타이핑된다. 콘솔이 포그라운드를 뺏으면 전 주문 거부 |
| 로그인·비밀번호는 다루지 않는다 | 로그아웃은 자동 복구 불가. 매시 점검 경보가 조기 경보 역할 |

운영 수칙: HTS 를 최소화하지 않고 최대화한 채 다른 창 뒤에 둔다(최소화 창은 점검이 복원하며 앞으로 나오고, 최대화 상태면 포그라운드를 바꾸지 않는다). PC 를 잠그지 않는다(입력 데스크톱이 Winlogon 으로 바뀌어 위와 같은 실패). always-on-top 앱을 띄우지 않는다. 절전·모니터 끄기·화면보호기·자동잠금은 전부 해제한다.

## 감시자·통제

| 결정 | 이유 |
|---|---|
| 감시자는 시각이 아니라 상태 기반 | 창(매도 15:10~15:19, 매수 15:21~15:28) 안에서 HTS 가 준비되는 순간 밀린 단계를 실행한다. 30초 폴링, 재시도 90초 |
| 주문 원장으로 중복 전송 차단 | 재시도 작업이 겹쳐도 같은 주문이 두 번 나가지 않는다. 남은 조각이 없으면 `connect()` 전에 종료 |
| 텔레그램 두 번 알림 — 15:08 "손 떼기", 단계 완료 시 "써도 된다"(늦어도 15:30) | 집행 창을 사람이 침범하지 않도록 |
| HTS 준비→미준비 전환 즉시 경보, 복귀도 알림 | 잠금·재로그인 대화상자를 조기 발견 |
| 텔레그램 명령(/상태 /정지 /재개 /건너뛰기 /익절 /복구 /제안 /상황판 …) | 상황판은 이 PC 안에서만 열린다. 설정된 chat_id 만 듣고, 상황판 손잡이와 같은 함수를 부른다 |
| 스케줄러 인자명 `$TaskArgs` | `$Args` 는 PowerShell 자동 변수와 충돌해 조용히 비고, `--live` 가 빠진 채 등록됐다 |

## 실패 처리

| 상황 | 처리 |
|---|---|
| 종목목록 출처 실패 | `data.load_listing()` 폴백 사슬: 캐시 CSV 의 있는 최근 날짜 → FDR → 네이버 시가총액 목록 → 로컬 `state/listing_cache.csv` |
| 텔레그램 429 | 대기 상한 10초(넘으면 포기·로그), 같은 글 20분 억제, 전송 기록 `state/notify_log.jsonl` |
| 시장가가 켜져 앵커 소실 | `execute.py --reset-form` (실측 절대좌표로 해제) |
| 주문 거부·미전송 | 15:24·15:27 재시도 |
| 상한가 스킵 | 비례 재분배 |
| 체결내역 파싱·주문 취소 | 미구현. `run.py reconcile` + 수동 `fill` 로 우회 |

## 알파와 백테스트의 신뢰도

- 정확한 표현은 "약한 횡단면 틸트를 20일 순위 대회의 보상 구조에 맞춰 2종목 고변동성 베팅으로 변환한 장치"다. 검증된 알파가 아니다.
- 알파가 2023년에 편중된다(이전 일평균 +1.009%/t 2.91, 2024년 이후 +0.541%/t 2.23). 경보필터를 빼면 t 1.92 로 5% 유의에 못 미친다.
- 다중검정 부채가 없는 증거는 무작위 대조 하나다: 1000회 중 신호를 이긴 시행 0/1000(p=0.001).
- 미래 참조 없음(`backtest.audit_lookahead()` 12개 시점 통과). 단 유니버스를 오늘 기준 거래대금 상위로 소급 적용한 생존편향은 남아 있다. 수수료 반영, 슬리피지는 시나리오로만.
- 재현: `python tools/verify_claims.py`, `tools/verify_alert.py`, `tools/verify_cost.py` → `state/backtest_current.json`.

## 1일차 사고

매매 0건. 원인은 시각이 아니라 데이터와 잠금이었다.

| 증상 | 원인 | 조치 |
|---|---|---|
| 15:05 계획 7회 재시도 전부 실패, 주문서 없음 | `fdr.StockListing("KRX")` 이 장 마감 뒤에야 생기는 당일 CSV 를 읽어 404 | `load_listing()` 폴백 사슬 |
| 09:30~14:50 점검마다 "화면잠금" 팝업 | 앱 자동잠금(IdleTimeout=0)이 아니라 외부에서 잠긴 세션. 재로그인은 자동화가 대신할 수 없다 | 준비→미준비 전환 즉시 경보. 수칙: PC·HTS 를 잠그지 않고 재부팅하지 않는다 |
| 계획 재시도가 6~9분씩 밀리고 경보가 늦게 닿음 | 텔레그램 429 의 retry_after 300~570초를 그대로 대기 | 대기 상한 10초, 20분 억제 |
| 밖에서 통제 불가 | 상황판이 로컬 전용 | 텔레그램 명령 스레드 추가 |

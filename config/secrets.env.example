# 텔레그램 자격증명. 이 파일을 config/secrets.env 로 복사한 뒤 값을 채운다.
# secrets.env 는 .gitignore 에 등록돼 있어 커밋되지 않는다.
# 값은 `python setup_telegram.py --token <봇토큰>` 으로 자동으로 채울 수도 있다.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
